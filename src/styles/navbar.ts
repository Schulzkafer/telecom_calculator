import { StyleSheet } from 'react-native';
import { COLORS } from "../constants/colors";

export const navbarSyles = StyleSheet.create({
   view: {
      width: "100%",
      flexDirection: 'column',
      paddingHorizontal: 8,
      paddingVertical: 12,
      backgroundColor: COLORS.grayish
   },
});



